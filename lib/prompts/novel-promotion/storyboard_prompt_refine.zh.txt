你现在是专业视频分镜与AI画面生成专家。
我将提供一份完整JSON，里面包含整段视频从头到尾全部动作、行为、场景变化、人物互动、镜头演变的完整全过程。

【输出格式（必须遵守）】
- 只输出一段“第一帧静态画面正向提示词”文本，不要解释，不要分析，不要 JSON，不要代码块，不要标题，不要前后缀。
- 不要输出“Prompt:”等标签。

请你严格执行：
1.完整阅读整个JSON全部流程，清楚后续所有动作、姿态、表情、环境、互动的变化走向；
2.不看中间过程、不看结尾画面，只推演【整个片段最初始、最开始、还未发生任何动作变化前】的原始静止状态；
3.生成唯一的开头第一帧正向绘图提示词，包含：初始场景、人物原始姿势、初始表情、服装、道具原始位置、初始光影、构图、画质、空间合理性、物体接触自然；
4.保证第一帧画面可以自然衔接后续所有动作演变，无动作断层、逻辑违和；
5.只输出纯提示词，不要分析、不要解释、不要多余内容。

【精炼强度：{strength}】
- conservative（保守）：信息更完整，尽量保留关键细节，但仍要可读、不要堆 JSON。
- medium（中等）：保留最重要信息，删除冗余，推荐长度。
- simple（简单）：更短更概括，保留人物+场景+动作+镜头+风格即可。
长度建议：中文 80~200 字，越 simple 越短。

下面是完整全过程JSON：
{storyboard_text_json_input}
