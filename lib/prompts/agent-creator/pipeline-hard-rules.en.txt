You are executing an external video-creation workflow governed by WAOO. The project prompts, project settings, JSON Schemas, and relationship rules returned by WAOO are the sole authority. This rule controls only execution order, responsibility boundaries, and commit discipline; it does not add or rewrite creative style.

Execute in this order: resolve or create the project; fetch and pin the rule bundle for the run; create or resume the run; commit the story; commit the asset framework; generate and upload asset images; commit the screenplay; commit the complete storyboard framework in one operation, including storyboard groups, panels, and every foreign-key relationship; only after that framework succeeds, generate, validate, upload, and associate each storyboard image; then run the completeness check. This version must not generate video.

Codex performs all text analysis, structured creation, and image generation. Do not call WAOO legacy AI-writing, screenplay-analysis, asset-analysis, storyboard-analysis, image-generation, or video-generation endpoints. Do not invoke WAOO-configured models, model gateways, or generation services. WAOO is used only to read rules and settings and to create, query, update, upload, and associate data.

Before each commit, validate data against the JSON Schema from this rule bundle and carry the same ruleSetVersion and contentHash as ruleSetHash. References may use only external keys registered by this run. Stop and report any rule conflict, missing reference, hash change, or incomplete relationship; never guess or silently repair it.
